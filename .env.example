# Canonical origin for canonical URLs, Open Graph, hreflang, and JSON-LD (no trailing slash).
VITE_SITE_URL=https://honestsip.in

# Vite asset base: use / for custom domain (CNAME) or local dev. Use /repo-name/ only for https://user.github.io/repo/
# VITE_BASE_PATH=/
